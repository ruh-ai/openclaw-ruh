{
  "name": "@ruh/openclaw-runtime",
  "version": "0.1.0",
  "description": "Runtime substrate for OpenClaw pipelines — the implementation of OpenClaw Spec v1.0.0-rc.1. Implements tool harness, error taxonomy, output validator, decision log, memory model, config substrate, checkpoint, hooks. Consumed by agent-builder-ui, ruh-backend, and customer pipeline deployments.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "spec:check": "bun run scripts/check-spec-conformance.ts"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=20.0.0"
  },
  "private": true
}
