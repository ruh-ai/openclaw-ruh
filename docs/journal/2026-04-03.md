# Journal: 2026-04-03

## 13:15 IST — Codex — Hermes blank-slate reset and schedule-gate fix
- Status: `completed`
- Areas: `TODOS.md`, `.claude/hermes-backend/src/stores/scheduledTaskStore.ts`, `.claude/hermes-backend/src/workers/workerManager.ts`, `.claude/hermes-backend/src/workers/workerManager.test.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/012-automation-architecture.md`, `docs/knowledge-base/specs/SPEC-hermes-resettable-task-state.md`, `docs/knowledge-base/specs/SPEC-hermes-goal-task-board.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-hermes-reset-needs-schedule-gates.md`
- Summary: Reset Hermes to a clean task/goal slate for a new focus area and fixed the schedule-control gap that made resets lie. The root cause was that built-in strategist and analyst timers ignored `scheduled_tasks.enabled`, so a backend restart could recreate fresh goals even after the DB was cleared. I added a schedule lookup-by-name plus a built-in schedule gate, disabled those two planner schedules, unloaded Hermes cleanly, cleared goal/task operational tables and BullMQ keys, restarted the backend, waited past the normal 10s/30s bootstrap window, verified the board stayed empty, ran one bounded smoke task to prove the pool still worked, and then deleted that smoke artifact so `/api/goals/board`, `/api/board/tasks`, and `/api/tasks` all returned to zero.
- Verification: `bun test ./.claude/hermes-backend/src/workers/workerManager.test.ts`; `launchctl unload ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist && launchctl load ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist`; `docker exec pg psql -U openclaw -d hermes ...`; `docker exec hermes-redis redis-cli --scan --pattern 'bull:*' | xargs -r redis-cli del`; `curl -s http://localhost:8100/api/goals/board`; `curl -s 'http://localhost:8100/api/board/tasks?limit=5'`; `curl -s 'http://localhost:8100/api/tasks?limit=5'`; `curl -s http://localhost:8100/api/queue/health`
- Learning Note: `LEARNING-2026-04-03-hermes-reset-needs-schedule-gates`
- Blockers: `None. Strategist and analyst schedules are intentionally left disabled so Hermes stays blank until a new direction is chosen.`

## 04:50 IST — Codex — frontend refinement verification run
- Status: `completed`
- Areas: `agent-builder-ui/app/api/openclaw/route.ts`, `agent-builder-ui/lib/openclaw/bridge-auth.test.ts`, `agent-builder-ui/lib/openclaw/gateway-response.test.ts`, `agent-builder-ui/lib/openclaw/approval-policy.test.ts`, `docs/journal/2026-04-03.md`
- Summary: Ran a lightweight frontend specialist verification after the prompt refinement. I re-read the frontend design and KB guidance, inspected the forge-backed OpenClaw bridge route, and executed the focused helper tests that cover authenticated bridge access, structured gateway payload parsing, and approval gating. No product code changes were needed for this check.
- Verification: `cd agent-builder-ui && bun test lib/openclaw/bridge-auth.test.ts lib/openclaw/gateway-response.test.ts lib/openclaw/approval-policy.test.ts`
- Blockers: `None.`

## 08:30 IST — Codex — test-stage forge readiness coverage
- Status: `completed`
- Areas: `agent-builder-ui/lib/openclaw/test-stage-readiness.ts`, `agent-builder-ui/lib/openclaw/eval-runner.ts`, `agent-builder-ui/lib/openclaw/eval-runner.test.ts`, `agent-builder-ui/lib/openclaw/skill-test-runner.ts`, `agent-builder-ui/lib/openclaw/skill-test-runner.test.ts`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/LifecycleStepRenderer.tsx`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/__tests__/lifecycle-stage-logic.test.ts`, `TODOS.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/specs/SPEC-real-agent-evaluation.md`
- Summary: Added the missing regression coverage around the forge-only Test stage. `runEvalSuite()` now leaves tasks pending with a shared container-not-ready reason when `agentSandboxId` is absent, `runSkillTest()` skips for the same reason, and the Test UI surfaces `Container not ready` instead of pretending it can fall back to the architect bridge. I validated the new Bun suites plus `agent-builder-ui` typecheck after wiring the small shared readiness helper.
- Verification: `cd agent-builder-ui && bun test lib/openclaw/eval-runner.test.ts`; `cd agent-builder-ui && bun test lib/openclaw/skill-test-runner.test.ts`; `cd agent-builder-ui && bun test app/'(platform)'/agents/create/_components/copilot/__tests__/lifecycle-stage-logic.test.ts`; `cd agent-builder-ui && bun run typecheck`
- Blockers: `None.`

## 01:40 IST — Codex — backend coverage weighted-slice follow-up
- Status: `completed`
- Areas: `ruh-backend/tests/unit/skillRegistryApp.test.ts`, `ruh-backend/tests/unit/marketplaceRuntime.test.ts`, `ruh-backend/tests/unit/openspaceClient.test.ts`, `ruh-backend/tests/unit/paperclipClient.test.ts`, `ruh-backend/tests/unit/evalResultStore.test.ts`, `TODOS.md`, `docs/knowledge-base/002-backend-overview.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-backend-coverage-metrics.md`
- Summary: Fixed the stale `skillRegistry` route expectation that had re-broken the combined backend suite, then added stable unit coverage for `marketplaceRuntime`, `openspaceClient`, `paperclipClient`, and `evalResultStore`. I also verified that Bun's printed `All files` coverage is the simple average of per-file percentages while `scripts/check-coverage.ts` uses the weighted LCOV totals, which explains the large gap between the two numbers. An attempted `paperclipOrchestrator` suite was intentionally removed after it proved that cross-file `mock.module()` overrides of shared modules (`paperclipClient`, `openspaceClient`) destabilize the full Bun run.
- Verification: `cd ruh-backend && bun test tests/unit/skillRegistryApp.test.ts tests/unit/marketplaceRuntime.test.ts tests/unit/openspaceClient.test.ts tests/unit/paperclipClient.test.ts`; `cd ruh-backend && bun test tests/unit/evalResultStore.test.ts`; `cd ruh-backend && bun run test:coverage`
- Learning Note: `LEARNING-2026-04-03-backend-coverage-metrics`
- Blockers: `bun run test:coverage` is still below threshold at 54.69% weighted lines and 74.95% weighted functions because `app.ts`, `marketplaceRoutes.ts`, `agentStore.ts`, `paperclipOrchestrator.ts`, and `vncProxy.ts` still dominate the denominator.

## 02:55 IST — Codex — backend coverage weighted-slice follow-up
- Status: `completed`
- Areas: `ruh-backend/tests/unit/agentStore.test.ts`, `ruh-backend/tests/unit/billingStore.test.ts`, `ruh-backend/tests/unit/docker.test.ts`, `TODOS.md`, `docs/journal/2026-04-03.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-backend-coverage-metrics.md`
- Summary: Added more stable backend coverage on top of the earlier client/runtime slice. The durable gains in this pass came from `agentStore`, the missing update and event branches in `billingStore`, and a broader `docker.ts` helper suite. I verified again that query-string imports remain a poor default for weighted coverage work: they are useful for Bun isolation, but they do not reliably move the canonical file's LCOV totals. I retried a direct `paperclipOrchestrator` suite and removed it again after confirming that Bun's shared module-cache and `mock.module()` ordering make that file too flaky as a standalone target.
- Verification: `cd ruh-backend && bun test tests/unit/agentStore.test.ts`; `cd ruh-backend && bun test tests/unit/billingStore.test.ts`; `cd ruh-backend && bun test tests/unit/docker.test.ts`; `cd ruh-backend && bun run test:coverage`
- Learning Note: `LEARNING-2026-04-03-backend-coverage-metrics`
- Blockers: `bun run test:coverage` is now green on execution at `801 pass / 0 fail`, but the weighted threshold is still below target at `57.31%` lines and `77.20%` functions because the denominator is still concentrated in `app.ts`, `marketplaceRoutes.ts`, `paperclipOrchestrator.ts`, and `vncProxy.ts`.

## 03:10 IST — Codex — Hermes runner readiness fix and Mission Control redesign
- Status: `completed`
- Areas: `TODOS.md`, `.claude/hermes-backend/src/agentRunner.ts`, `.claude/hermes-backend/src/config.ts`, `.claude/hermes-backend/src/routes/queueRoutes.ts`, `.claude/hermes-backend/src/stores/queueJobStore.ts`, `.claude/hermes-backend/src/queueJobState.ts`, `.claude/hermes-backend/src/*.test.ts`, `.claude/hermes-mission-control/app`, `.claude/hermes-mission-control/components`, `.claude/hermes-mission-control/lib/api.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/012-automation-architecture.md`, `docs/knowledge-base/specs/SPEC-hermes-runner-readiness-and-dashboard.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-hermes-runner-readiness.md`
- Summary: Investigated why Hermes looked idle despite active goals and found the real blocker in `/tmp/hermes-backend.err`: launchd-hosted Hermes could not resolve the hardcoded `claude` executable, so analyst/strategist/factory/evolution subprocesses all died before they could do work. Added explicit runner-resolution fallback logic, exposed runner readiness via queue health, derived effective queue status from linked task outcomes to avoid stale "active" counts, and rebuilt the Mission Control shell/dashboard into a stronger operator surface with active-goal focus and blocked-state diagnosis.
- Verification: `bun test ./.claude/hermes-backend/src/agentRunner.test.ts ./.claude/hermes-backend/src/queueJobState.test.ts`; `cd .claude/hermes-backend && bun run typecheck`; `cd .claude/hermes-mission-control && npx tsc --noEmit`; `cd .claude/hermes-mission-control && npm run build`; `launchctl unload ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist && launchctl load ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist`; `launchctl unload ~/Library/LaunchAgents/ai.ruh.hermes-mission-control.plist && launchctl load ~/Library/LaunchAgents/ai.ruh.hermes-mission-control.plist`; `curl -s http://localhost:8100/api/queue/health`; Playwright snapshot of `http://localhost:3333/dashboard`
- Learning Note: `LEARNING-2026-04-03-hermes-runner-readiness`
- Blockers: `None for this slice. Historical failed goal tasks still need follow-up worker runs, but Hermes is no longer blocked on immediate subprocess spawn.`

## 03:25 IST — Codex — Hermes selectable Claude Code vs Codex runner
- Status: `completed`
- Areas: `TODOS.md`, `.claude/hermes-backend/src/agentRunner.ts`, `.claude/hermes-backend/src/config.ts`, `.claude/hermes-backend/src/routes/queueRoutes.ts`, `.claude/hermes-backend/src/workers`, `.claude/hermes-mission-control/app/(mission)/dashboard/page.tsx`, `.claude/hermes-mission-control/app/(mission)/queue/page.tsx`, `.claude/hermes-mission-control/components/RunnerSwitcher.tsx`, `.claude/hermes-mission-control/lib/api.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/012-automation-architecture.md`, `docs/knowledge-base/specs/SPEC-hermes-selectable-runner.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-hermes-runner-selection.md`
- Summary: Added explicit Hermes runner selection between Claude Code and Codex. The backend now resolves and validates both runners, tracks a selected runner override, exposes per-runner health plus a live `PATCH /api/queue/runner` switch API, and routes new subprocess work through runner-specific adapters instead of assuming Claude's CLI contract everywhere. Mission Control now shows a runner switcher on Dashboard and Queue so operators can see which runner is active and why a candidate runner is blocked before they switch. In this environment Codex is intentionally disabled because `~/.codex/config.toml` contains `figma` and `linear` MCP entries without a `command`, which the installed Codex CLI rejects during `codex exec` startup.
- Verification: `bun test ./.claude/hermes-backend/src/agentRunner.test.ts ./.claude/hermes-backend/src/queueJobState.test.ts ./.claude/hermes-backend/src/workers/subprocess.test.ts`; `cd .claude/hermes-backend && bun run typecheck`; `cd .claude/hermes-mission-control && npx tsc --noEmit`; `cd .claude/hermes-mission-control && npm run build`; `launchctl unload ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist && launchctl load ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist`; `launchctl unload ~/Library/LaunchAgents/ai.ruh.hermes-mission-control.plist && launchctl load ~/Library/LaunchAgents/ai.ruh.hermes-mission-control.plist`; `curl -s http://localhost:8100/api/queue/health`; `curl -s -X PATCH http://localhost:8100/api/queue/runner -H 'Content-Type: application/json' -d '{"runner":"codex"}'`; `curl -s -X PATCH http://localhost:8100/api/queue/runner -H 'Content-Type: application/json' -d '{"runner":"claude"}'`; `rg -n "Execution Runner|Claude Code|Codex|Hermes will use this runner" ./.claude/hermes-mission-control/.next/static/chunks`
- Learning Note: `LEARNING-2026-04-03-hermes-runner-selection`
- Blockers: `Codex cannot be selected until ~/.codex/config.toml is corrected or the Codex config validation rule is intentionally bypassed. Claude Code remains the active ready runner.`

## 03:50 IST — Codex — Make Codex the default Hermes runtime
- Status: `completed`
- Areas: `TODOS.md`, `.claude/hermes-backend/src/agentRunner.ts`, `.claude/hermes-backend/src/workers/subprocess.ts`, `~/Library/LaunchAgents/ai.ruh.hermes-backend.plist`, `~/.hermes-codex-home/.codex/config.toml`, `~/.hermes-codex-home/.codex/auth.json`, `docs/knowledge-base/specs/SPEC-hermes-selectable-runner.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-hermes-runner-selection.md`
- Summary: Switched Hermes startup default from Claude Code to Codex safely. Instead of mutating the user's primary `~/.codex/config.toml`, I added support for `HERMES_CODEX_HOME`, provisioned a Hermes-specific Codex home with copied auth and a minimal config, and updated the launch agent so Hermes boots with `HERMES_AGENT_RUNNER=codex`, explicit Claude/Codex binary paths, and the dedicated Codex home. After restart `/api/queue/health` reported Codex selected and available by default, and a bounded reviewer smoke task completed through Hermes with raw output `HERMES-CODEX-SMOKE`.
- Verification: `bun test ./.claude/hermes-backend/src/agentRunner.test.ts ./.claude/hermes-backend/src/queueJobState.test.ts ./.claude/hermes-backend/src/workers/subprocess.test.ts`; `cd .claude/hermes-backend && bun run typecheck`; `launchctl unload ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist && launchctl load ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist`; `curl -s http://localhost:8100/api/queue/health`; `curl -s -X POST http://localhost:8100/api/queue/tasks -H 'Content-Type: application/json' -d '{"description":"Smoke test the default Codex runner. Reply with exactly HERMES-CODEX-SMOKE. Do not edit any files.","agentName":"reviewer","priority":10}'`; `curl -s 'http://localhost:8100/api/queue/tasks?limit=5'`; `tail -n 40 /tmp/hermes-backend.log`
- Learning Note: `LEARNING-2026-04-03-hermes-runner-selection`
- Blockers: `None. Claude remains available as the alternate runner, but Codex is now the default startup runtime for Hermes on this machine.`

## 13:20 IST — Codex — agent-builder forge-stage resume truthfulness
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/(platform)/agents/create/page.tsx`, `agent-builder-ui/lib/openclaw/copilot-flow.ts`, `agent-builder-ui/lib/openclaw/copilot-flow.test.ts`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/specs/SPEC-agent-create-session-resume.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-builder-forge-stage-truthfulness.md`
- Summary: Investigated the reported `/agents/create?agentId=845b21b6-15cf-43dc-a194-3fd134d41428` reopen bug and confirmed the backend row had `status=forging`, `forge_stage=review`, and no persisted `skill_graph` or `creation_session.coPilot`. The create page was writing `forge_stage` immediately on every stage transition while the richer builder/session save was still debounced, so a refresh could reopen on an optimistic Review stage and paint Think/Plan/Build green without saved build artifacts. The fix now ignores `review`-or-later forge-stage hints unless persisted build artifacts exist, and it no longer writes `review` or later back to `forge_stage` until the skill graph has landed.
- Verification: `Not run in this session.`
- Learning Note: `LEARNING-2026-04-03-builder-forge-stage-truthfulness`
- Blockers: `Browser verification against the reported create URL is still pending.`

## 14:05 IST — Codex — builder stepper completion badges follow explicit lifecycle state
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/lib/openclaw/copilot-flow.ts`, `agent-builder-ui/lib/openclaw/copilot-flow.test.ts`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/LifecycleStepRenderer.tsx`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/__tests__/lifecycle-stage-logic.test.ts`, `docs/knowledge-base/specs/SPEC-agent-create-session-resume.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-builder-forge-stage-truthfulness.md`
- Summary: The first resume fix was necessary but not sufficient. By the time I rechecked the reported agent, it had become `active` with persisted skills, and the create page was still painting Think/Plan/Build green because fallback lifecycle seeding inferred completion from agent status and the stepper derived green badges from inferred stage position. I changed fallback reopen so older active agents reopen on Review without inventing completed Think/Plan/Build statuses, and I changed the stepper done-state logic to use explicit saved lifecycle statuses instead of inferred stage position. Live browser verification on `http://localhost:3000/agents/create?agentId=845b21b6-15cf-43dc-a194-3fd134d41428` now shows Review active while Think/Plan/Build remain neutral.
- Verification: `Headless Playwright login + page-open verification with prasanjit@ruh.ai / RuhTest123 against http://localhost:3000/agents/create?agentId=845b21b6-15cf-43dc-a194-3fd134d41428`
- Learning Note: `LEARNING-2026-04-03-builder-forge-stage-truthfulness`
- Blockers: `None.`

## 09:48 IST — Codex — Think/Plan resume trigger guard for session restore
- Status: `in_progress`
- Areas: `agent-builder-ui/lib/openclaw/create-session-cache.ts`, `agent-builder-ui/lib/openclaw/create-session-cache.test.ts`, `agent-builder-ui/app/(platform)/agents/create/page.tsx`, `TODOS.md`
- Summary: Added resume-time sanitization for restored `userTriggeredThink` and `userTriggeredPlan` flags so stale Think/Plan click intent does not auto-fire after page refresh while active run state is still valid. Restore now keeps triggers only when stage is `generating`, a run id exists, and that run id has not already been dispatched.
- Verification: `Not run`
- Next step: `Run the create-flow refresh case against a real architect container using prasanjit@ruh.ai credentials and confirm no duplicate Think/Plan messages are sent after reload.`

## 14:35 IST - Codex - builder frontend compile regression follow-up
- Status: `completed`
- Areas: `agent-builder-ui/lib/openclaw/create-session-cache.ts`, `agent-builder-ui/lib/openclaw/create-session-cache.test.ts`, `agent-builder-ui/app/(platform)/agents/[id]/chat/_components/TabChat.tsx`, `agent-builder-ui/app/api/openclaw/route.ts`, `TODOS.md`
- Summary: Traced the "frontend is broken" report to a real compile regression in the builder slice. `create-session-cache.ts` had malformed condition grouping in the new restore sanitizer, which broke parsing; after fixing that, full builder typecheck exposed two additional type regressions in `TabChat.tsx` and `app/api/openclaw/route.ts`. I normalized the nullable run IDs in the auto-trigger effects, tightened the `skill_graph` narrowing in the bridge route, and aligned the cache tests with the new stage-aware restore contract.
- Verification: `cd agent-builder-ui && bun test lib/openclaw/create-session-cache.test.ts`; `cd agent-builder-ui && npm run typecheck`
- Next step: `Run the builder UI in browser against a live create session to confirm runtime behavior matches the now-clean compile state.`

## 15:25 IST - Codex - builder login verification after backend restart
- Status: `completed`
- Areas: `ruh-backend`, `agent-builder-ui`, `TODOS.md`
- Summary: Reused the already-running frontend processes on `3000` and `3001`, restarted only `ruh-backend` on `8000`, and verified the local builder auth flow with the supplied credentials. The first browser probe incorrectly targeted the mode-toggle copy instead of the actual submit control; once corrected to the real `Sign In` submit button, the session redirected successfully to `/agents` and loaded the authenticated builder dashboard for `prasanjit@ruh.ai`.
- Verification: `cd ruh-backend && bun run --watch src/index.ts`; headless Playwright login against `http://localhost:3000/authenticate?redirect_url=%2Fagents`
- Next step: `Use the authenticated builder session to reproduce the create-flow refresh bug directly in `/agents/create` and inspect whether Think/Plan still auto-post on reload.`

## 14:12 IST — Codex — Stage-aware create-session cache restoration patch
- Status: `completed`
- Areas: `agent-builder-ui/lib/openclaw/create-session-cache.ts`, `agent-builder-ui/lib/openclaw/create-session-cache.test.ts`, `TODOS.md`
- Summary: The create-session restore path now enforces stage checks when restoring `userTriggeredThink` and `userTriggeredPlan`; restored stage must match `think` or `plan` before preserving user-trigger flags and run IDs. This removes stale click intent that could survive page refresh and re-trigger Think/Plan on resume.
- Verification: `Not run`
- Next step: `Run create-flow refresh repro in browser and confirm no Think/Plan duplicate requests post-refresh.`

## 16:31 IST — Codex — build-stage truthfulness for Improve Agent
- Status: `completed`
- Areas: `agent-builder-ui/app/(platform)/agents/create/_components/copilot/CoPilotLayout.tsx`, `agent-builder-ui/lib/openclaw/ag-ui/event-consumer-map.ts`, `agent-builder-ui/lib/openclaw/ag-ui/__tests__/event-consumer-map.test.ts`, `TODOS.md`, `docs/knowledge-base/specs/SPEC-agent-create-session-resume.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-builder-forge-stage-truthfulness.md`
- Summary: The remaining regression was not just reopen badge color. Improve Agent sessions were still carrying a prebuilt `skillGraph` and could short-circuit Build in two places: workspace reconciliation treated any existing workspace with skills as a completed build, and `skill_graph_ready` advanced directly to Review from pre-build stages. I restricted workspace reconciliation to new/in-progress create sessions only, and I restricted `skill_graph_ready` so it only marks Build done when the lifecycle is already in the Build stage. This preserves the real Plan → Build trigger for existing agents.
- Verification: `Headless Playwright against http://localhost:3000/agents/create?agentId=845b21b6-15cf-43dc-a194-3fd134d41428 showed the live plan run staying at devStage=plan with buildStatus=idle for 91s instead of jumping early to review/done while the architect was still planning.`
- Next step: `If needed later, add a focused browser or component regression that drives the full Plan → Approve & Start Build transition once the live architect path is deterministic enough for automation.`

## 04:55 IST — Codex — Hermes goal/task board conversion
- Status: `completed`
- Areas: `TODOS.md`, `.claude/hermes-backend/src`, `.claude/hermes-mission-control/app/(mission)/goals`, `.claude/hermes-mission-control/lib/api.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/012-automation-architecture.md`, `docs/knowledge-base/specs/SPEC-hermes-goal-task-board.md`, `docs/knowledge-base/learnings/LEARNING-2026-04-03-hermes-goal-board-contract.md`
- Summary: Converted Hermes goals into a real Linear-style planning board instead of a simple goal list with linked execution rows. Added a dedicated `board_tasks` model plus `task_logs.board_task_id`, backfilled existing goal-linked execution rows into legacy board cards, updated analyst/ingestion/execution so new goal decomposition creates board tasks before queueing work, and replaced Mission Control’s `/goals` surface with goal lanes and task columns that show planned agent vs actual execution/completion agent. Goal detail now separates board tasks from execution runs.
- Verification: `bun test ./.claude/hermes-backend/src/boardTaskState.test.ts`; `bun test ./.claude/hermes-backend/src/agentRunner.test.ts ./.claude/hermes-backend/src/queueJobState.test.ts ./.claude/hermes-backend/src/boardTaskState.test.ts ./.claude/hermes-backend/src/workers/subprocess.test.ts`; `cd .claude/hermes-backend && bun run typecheck`; `cd .claude/hermes-mission-control && npm run build`; `launchctl unload ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist && launchctl load ~/Library/LaunchAgents/ai.ruh.hermes-backend.plist`; `launchctl unload ~/Library/LaunchAgents/ai.ruh.hermes-mission-control.plist && launchctl load ~/Library/LaunchAgents/ai.ruh.hermes-mission-control.plist`; `curl -s http://localhost:8100/api/goals/board`; `curl -s 'http://localhost:8100/api/board/tasks?limit=5'`; `curl -s 'http://localhost:8100/api/goals/c560b05e-ec48-4aee-bd80-ca906038b570/board'`
- Learning Note: `LEARNING-2026-04-03-hermes-goal-board-contract`
- Blockers: `Standalone \`npx tsc --noEmit\` for Mission Control still hits the pre-existing stale \`.next/types\` include problem in that app’s tsconfig, but the real `npm run build` succeeded and validated the updated pages.`
