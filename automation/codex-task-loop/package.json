{
  "name": "@ruh/codex-task-loop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Single-worker automation loop for OpenClaw-driven Codex task execution.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
