export default function MarketplacePage() {
  return (
    <div>
      <h1 className="text-lg font-bold text-[var(--text-primary)]">Marketplace</h1>
      <p className="text-xs text-[var(--text-tertiary)] mt-0.5">Agent submissions and moderation</p>
      <div className="mt-6 flex items-center justify-center py-20 bg-[var(--card-color)] rounded-xl border border-[var(--border-default)]">
        <p className="text-xs text-[var(--text-tertiary)]">Marketplace moderation will be available after the Employee Marketplace is built (Phase 3).</p>
      </div>
    </div>
  );
}
