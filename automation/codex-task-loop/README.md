# Codex Task Loop Runner

This package contains the deterministic runner used by the OpenClaw cron-driven task loop on the GCP VM.

## Current Scope

- validate runtime configuration
- emit structured startup logs
- provide the package scaffold for lease, Linear, Codex, and GitHub orchestration

## Commands

- `npm run build`
- `npm test`
