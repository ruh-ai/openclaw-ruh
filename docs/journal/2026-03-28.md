# Journal: 2026-03-28

## 00:33 IST — Analyst-1 — Co-Pilot draft channel persistence gap
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/lib/openclaw/ag-ui/builder-metadata-autosave.ts`, `agent-builder-ui/app/(platform)/agents/create/page.tsx`, `agent-builder-ui/hooks/use-agents-store.ts`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/specs/SPEC-agent-builder-channel-persistence.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-28-copilot-channel-draft-persistence-gap.md`
- Summary: Reviewed the active focus lane, existing TODO coverage, and the live create-flow draft path to identify one unowned feature package. Added TASK-2026-03-28-188 after confirming the saved-agent/store contract already supports `channels[]` but the AG-UI draft autosave payload still omits live channel selections, which makes `Draft saved` overstate what the default Co-Pilot path actually preserves across refresh or resume.
- Verification: `sed -n '1,260p' docs/knowledge-base/000-INDEX.md`; `sed -n '1,260p' TODOS.md`; `sed -n '1,260p' docs/project-focus.md`; `sed -n '1,340p' agent-builder-ui/lib/openclaw/ag-ui/builder-metadata-autosave.ts`; `rg -n "saveAgentDraft|channels" agent-builder-ui/hooks/use-agents-store.ts ruh-backend/src/agentStore.ts`
- Learning Note: [[LEARNING-2026-03-28-copilot-channel-draft-persistence-gap]]
- Blockers: `None`

## 00:39 IST — Worker-1 — Sandbox runtime reconciliation
- Status: `completed`
- Areas: `TODOS.md`, `ruh-backend/src/app.ts`, `ruh-backend/src/docker.ts`, `ruh-backend/src/sandboxRuntime.ts`, `ruh-backend/tests/unit/docker.test.ts`, `ruh-backend/tests/unit/sandboxRuntime.test.ts`, `ruh-backend/tests/unit/sandboxReconciliationApp.test.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/003-sandbox-lifecycle.md`, `docs/knowledge-base/004-api-reference.md`, `docs/knowledge-base/005-data-models.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-sandbox-runtime-reconciliation.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-25-sandbox-runtime-drift.md`
- Summary: Completed TASK-2026-03-25-31 as one bounded backend feature package. Added managed-container inventory parsing plus a pure reconciliation module, taught `GET /api/sandboxes/:sandbox_id/status` to return explicit drift metadata instead of a plain DB fallback, and exposed admin-only reconcile plus repair routes for `db_only` and `container_only` drift. The KB now documents the contract through [[SPEC-sandbox-runtime-reconciliation]] and the existing sandbox-runtime-drift learning note links to the shipped spec.
- Verification: `cd ruh-backend && bun test tests/unit/docker.test.ts`; `cd ruh-backend && bun test tests/unit/sandboxRuntime.test.ts tests/unit/sandboxReconciliationApp.test.ts`
- Learning Note: `updated` [[LEARNING-2026-03-25-sandbox-runtime-drift]]
- Blockers: `The existing supertest HTTP harness in `ruh-backend/tests/e2e/chatProxy.test.ts` still fails before requests execute with `app.address().port`, so this run used the repo's unit app-route harness for backend route verification instead of route-level e2e coverage.`

## 01:25 IST — Codex — Repo-wide testability audit
- Status: `completed`
- Areas: `TODOS.md`, `docs/journal/2026-03-28.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-28-repo-testability-audit.md`, `docs/knowledge-base/001-architecture.md`, `docs/knowledge-base/002-backend-overview.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/009-ruh-frontend.md`
- Summary: Completed a static deep-dive on what makes this repo costly to test. The dominant problems are monolithic backend and bridge modules, heavy React page/hook orchestration in the builder flow, state stores that mix persistence/network/UI concerns, repeated direct use of global runtime dependencies, and an implicit `agent-builder-ui` unit-test workflow that relies on Bun globals without a documented package command.
- Verification: `sed -n '1,260p' docs/knowledge-base/000-INDEX.md`; `sed -n '1,240p' TODOS.md`; `find ruh-backend/src agent-builder-ui ruh-frontend -type f \( -name '*.ts' -o -name '*.tsx' \) -print0 | xargs -0 wc -l | sort -nr | head -n 60`; `rg -n "app\\.(get|post|put|patch|delete)\\(" ruh-backend/src/app.ts`; `sed -n '1,260p' ruh-backend/src/sandboxManager.ts`; `sed -n '1,240p' agent-builder-ui/app/api/openclaw/route.ts`; `sed -n '1,240p' agent-builder-ui/lib/openclaw/ag-ui/use-agent-chat.ts`; `sed -n '1,260p' agent-builder-ui/hooks/use-agents-store.ts`; `sed -n '1,220p' ruh-frontend/components/ChatPanel.tsx`; `sed -n '1,240p' ruh-frontend/components/ChannelsPanel.tsx`; `sed -n '1,220p' agent-builder-ui/app/api/openclaw/route.test.ts`; `sed -n '1,220p' ruh-backend/tests/e2e/chatProxy.test.ts`
- Learning Note: [[LEARNING-2026-03-28-repo-testability-audit]]
- Blockers: `None. No tests were run because this task was a static audit rather than an implementation pass.`

## 01:41 IST — Codex — Refresh the Obsidian KB against the live repo
- Status: `completed`
- Areas: `TODOS.md`, `docs/journal/2026-03-28.md`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/001-architecture.md`, `docs/knowledge-base/002-backend-overview.md`, `docs/knowledge-base/003-sandbox-lifecycle.md`, `docs/knowledge-base/004-api-reference.md`, `docs/knowledge-base/005-data-models.md`, `docs/knowledge-base/006-channel-manager.md`, `docs/knowledge-base/007-conversation-store.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/009-ruh-frontend.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-google-ads-agent-creation-loop.md`, `docs/knowledge-base/specs/SPEC-tool-integration-workspace.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-27-tool-research-plan-persistence-gap.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-27-operator-defined-runtime-input-gap.md`
- Summary: Completed a repo-wide KB refresh pass against the live working tree. The core notes now describe the shipped backend/frontend contract instead of mixing in older route shapes or intended behavior: sandbox/API notes cover forge, restart, runtime-env, browser/VNC, preview proxy, paginated history, and the developer chat `POST /chat/ws` path; data-model and builder notes now reflect live `channels[]`, `forge_sandbox_id`, `vnc_port`, current auth behavior, and the fact that `toolConnections[].researchPlan` plus `runtimeInputs[].source = operator_defined` are still planned rather than shipped. The pass also repaired the filtered wikilink graph so the current KB has zero broken links and zero orphan notes after excluding template placeholders.
- Verification: `rg -n "app\\.(get|post|put|patch|delete|all)\\(" ruh-backend/src/app.ts`; `sed -n '1,260p' docs/knowledge-base/000-INDEX.md`; `sed -n '1,320p' docs/knowledge-base/004-api-reference.md`; `sed -n '760,1180p' docs/knowledge-base/004-api-reference.md`; `sed -n '1,260p' docs/knowledge-base/001-architecture.md`; `sed -n '1,260p' docs/knowledge-base/003-sandbox-lifecycle.md`; `sed -n '1,260p' docs/knowledge-base/007-conversation-store.md`; `sed -n '1,260p' docs/knowledge-base/010-deployment.md`; `sed -n '1,240p' agent-builder-ui/middleware.ts`; `sed -n '1,240p' agent-builder-ui/components/auth/SessionInitializationWrapper.tsx`; `sed -n '1,520p' ruh-frontend/components/ChatPanel.tsx`; `node` KB graph scan (filtered placeholders) → `brokenCount: 0`, `orphanCount: 0`; filtered backlink scan still reports legacy debt (`missingBacklinkCount: 646`)`
- Learning Note: `updated` [[LEARNING-2026-03-27-tool-research-plan-persistence-gap]] and [[LEARNING-2026-03-27-operator-defined-runtime-input-gap]]
- Blockers: `The filtered graph has no broken links or orphans now, but the repo still carries large pre-existing backlink debt (646 missing reverse links in the strict scan). This pass fixed truthfulness, coverage, and discoverability for the core notes/specs; it did not normalize every historical backlink pair in the learning/spec archive.`

## 02:04 IST — Codex — Builder bridge testability refactor slice
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/api/openclaw/route.ts`, `agent-builder-ui/lib/openclaw/bridge-auth.ts`, `agent-builder-ui/lib/openclaw/approval-policy.ts`, `agent-builder-ui/lib/openclaw/gateway-response.ts`, `agent-builder-ui/lib/openclaw/*.test.ts`, `agent-builder-ui/lib/openclaw/ag-ui/builder-agent.ts`, `agent-builder-ui/app/(platform)/agents/create/page.tsx`, `agent-builder-ui/package.json`, `agent-builder-ui/tsconfig.json`, `agent-builder-ui/e2e/create-agent.spec.ts`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-28-agent-builder-test-bucketing-and-auth-fixture.md`
- Summary: `Completed TASK-2026-03-28-192 as the first bounded refactor from the repo-wide testability audit. The shared `/api/openclaw` bridge now delegates session validation, approval evaluation, and final gateway payload parsing to focused helper modules with dedicated Bun unit tests; `agent-builder-ui/package.json` now exposes a real npm-backed unit-test contract that isolates Bun module-mock suites into separate buckets; verification-driven cleanup fixed current production blockers including a duplicate builder state declaration, TS drift in trigger/config helpers, optional review-card edit props, and the required Suspense boundary around `/agents/create` search params. The mocked Playwright create-flow fixture now seeds auth cookies and `/users/me` so non-dev builder routes can be exercised under the active auth gate.`
- Verification: `cd agent-builder-ui && bun test app/api/openclaw/route.test.ts lib/openclaw/response-normalization.test.ts lib/openclaw/api.test.ts lib/openclaw/bridge-auth.test.ts lib/openclaw/approval-policy.test.ts lib/openclaw/gateway-response.test.ts`; `cd agent-builder-ui && npm test`; `cd agent-builder-ui && npm run typecheck`; `cd agent-builder-ui && PATH="/tmp/codex-bin:$PATH" npm run build`; `cd agent-builder-ui && npx playwright test e2e/create-agent.spec.ts --project=chromium -g "renders chat view with builder mode header"`; `cd agent-builder-ui && npx playwright test e2e/create-agent.spec.ts --project=chromium -g "sends a message and receives architect response"`; `cd agent-builder-ui && npx playwright test e2e/create-agent.spec.ts --project=chromium` → `3 passed, 13 failed, 9 did not run`
- Learning Note: [[LEARNING-2026-03-28-agent-builder-test-bucketing-and-auth-fixture]]
- Blockers: `The delivered bridge/testability slice is green, but the historical full create-agent Playwright file is not yet truthful for the current UI. Older assertions still expect disabled runtime tabs plus the previous review/config progression, and a worker restart during the broad suite hit `Cannot find module 'playwright-core/lib/utils'`, so full-browser confidence still needs a dedicated suite-refresh pass outside this bounded refactor.`

## 02:05 IST — Codex — Agent-readable system events and Langfuse bridge tracing
- Status: `completed`
- Areas: `TODOS.md`, `docs/journal/2026-03-28.md`, `docs/knowledge-base/specs/SPEC-agent-readable-system-events.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-28-agent-readable-system-events.md`, `docs/knowledge-base/001-architecture.md`, `docs/knowledge-base/002-backend-overview.md`, `docs/knowledge-base/003-sandbox-lifecycle.md`, `docs/knowledge-base/004-api-reference.md`, `docs/knowledge-base/005-data-models.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/010-deployment.md`, `ruh-backend/src/app.ts`, `ruh-backend/src/schemaMigrations.ts`, `ruh-backend/src/systemEventStore.ts`, `ruh-backend/tests/helpers/db.ts`, `ruh-backend/tests/unit/systemEventStore.test.ts`, `ruh-backend/tests/unit/systemEventsApp.test.ts`, `agent-builder-ui/app/api/openclaw/route.ts`, `agent-builder-ui/lib/openclaw/langfuse.ts`, `agent-builder-ui/lib/openclaw/langfuse.test.ts`, `agent-builder-ui/package.json`, `agent-builder-ui/package-lock.json`, `agent-builder-ui/yarn.lock`
- Summary: `Completed the first hybrid observability slice for agent-readable logs. The backend now owns a redacted `system_events` ledger with bounded read APIs and stable request correlation, and sandbox-create / forge lifecycle flows persist durable rows instead of relying only on transient SSE text. The Node-based architect bridge now adds optional Langfuse/OpenTelemetry tracing for accepted runs, retries, approvals, socket failures, and final outcomes, while keeping bridge behavior unchanged when Langfuse is not configured. The KB/specs now document the shipped routes, schema, env vars, and the rule that Langfuse is additive rather than the canonical runtime log.`
- Verification: `cd ruh-backend && bun test tests/unit/systemEventStore.test.ts tests/unit/systemEventsApp.test.ts`; `cd agent-builder-ui && bun test app/api/openclaw/route.test.ts lib/openclaw/langfuse.test.ts`; `cd agent-builder-ui && npm run typecheck`; `cd agent-builder-ui && npm run build`
- Learning Note: [[LEARNING-2026-03-28-agent-readable-system-events]]
- Blockers: `None.`

## 02:21 IST — Codex — Local system bring-up probe
- Status: `completed`
- Areas: `TODOS.md`, `docs/journal/2026-03-28.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-28-local-builder-bridge-auth-gap.md`
- Summary: `Started the repo's live services to verify the current runtime state instead of relying only on tests. `ruh-backend` booted on `http://localhost:8000` against the local Postgres container, `ruh-frontend` booted on `http://localhost:3000`, and `agent-builder-ui` booted on `http://localhost:3001` using `.env.local`; the builder create page renders in development mode. The main runtime gap is the shared builder bridge: without a cookie it returns `401 unauthorized`, and even with `accessToken=...` it returns `503 auth_unavailable` because the local backend does not implement `GET /users/me`, which `app/api/openclaw/route.ts` now requires for bridge session validation.`
- Verification: `docker ps`; `lsof -nP -iTCP -sTCP:LISTEN | rg ':(3000|3001|8000|5432)\\b'`; `cd ruh-backend && bun run start`; `cd ruh-frontend && PORT=3000 NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev`; `cd agent-builder-ui && PORT=3001 NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev`; `curl http://localhost:8000/api/agents`; `curl http://localhost:8000/api/sandboxes`; `curl http://localhost:3000/`; `curl http://localhost:3001/agents/create`; `curl -X POST http://localhost:3001/api/openclaw ...` → `401`; `curl -X POST http://localhost:3001/api/openclaw -H 'cookie: accessToken=local-dev-token' ...` → `{"error":"auth_unavailable","detail":"Session validation failed with status 404."}`
- Learning Note: [[LEARNING-2026-03-28-local-builder-bridge-auth-gap]]
- Blockers: `The builder chat loop cannot run locally end to end against this backend alone until the `/users/me` session-validation dependency is satisfied or explicitly bypassed in dev.`

## 03:58 IST — Codex — Local Langfuse Docker deployment
- Status: `completed`
- Areas: `TODOS.md`, `docs/journal/2026-03-28.md`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/001-architecture.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/010-deployment.md`, `docs/knowledge-base/specs/SPEC-local-langfuse-docker.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-28-local-langfuse-docker-bootstrap.md`, `langfuse/.env.example`, `langfuse/docker-compose.yml`, `langfuse/README.md`, `scripts/langfuse-local.sh`, `agent-builder-ui/.env.example`
- Summary: `Completed the repo-local Langfuse deployment so builder traces can be inspected without an external SaaS target. The repo now ships an isolated `langfuse/` Docker Compose stack and a wrapper script that generates ignored bootstrap secrets, writes the builder `LANGFUSE_*` overlay, and manages `up/down/status/reset`. A live bring-up found a real host-port collision on `5433` from an unrelated `devpulse_postgres` container, so the script was hardened to detect active listeners with `lsof` and auto-bump to the next free port; on this machine Langfuse Postgres is now bound to `127.0.0.1:5434` while the UI remains at `http://localhost:3002`. Browser verification confirmed the UI redirects to sign-in, the seeded `langfuse@local.dev` credentials work, and the headless bootstrap created the `openclaw-local` organization plus `openclaw-ruh-enterprise` project.`
- Verification: `./scripts/langfuse-local.sh reset`; `./scripts/langfuse-local.sh up`; `docker compose -f langfuse/docker-compose.yml --env-file langfuse/.env ps`; `curl --max-time 10 -sS -D - http://localhost:3002 -o /tmp/langfuse-home.html`; Playwright: navigate to `http://localhost:3002`, sign in as `langfuse@local.dev`, confirm `Organizations | Langfuse` with `openclaw-local` and `openclaw-ruh-enterprise``; `cat langfuse/.env`; `cat agent-builder-ui/.env.development.local`
- Learning Note: [[LEARNING-2026-03-28-local-langfuse-docker-bootstrap]]
- Blockers: `None. Operators only need to restart `agent-builder-ui` if it was already running before the new `LANGFUSE_*` overlay was written.`

## 04:07 IST — Codex — Empty local Langfuse investigation
- Status: `completed`
- Areas: `TODOS.md`, `docs/journal/2026-03-28.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-28-local-builder-bridge-auth-gap.md`
- Summary: `Investigated why the freshly deployed local Langfuse project still showed no data. The live `agent-builder-ui` dev server on port `3001` was started at `03:36 IST`, before `agent-builder-ui/.env.development.local` was generated at `03:58 IST`, so the running Next.js process had no `LANGFUSE_*` env and could not export anything. Separately, the shared `/api/openclaw` route only creates a Langfuse observation after `requireAuthenticatedBridgeSession()` succeeds; in the current repo-only local setup that still fails on backend `GET /users/me`, so `/api/openclaw` returns `auth_unavailable` before any trace is started. This means empty local Langfuse is expected until the builder is restarted and a real authenticated architect request succeeds.`
- Verification: `ps -Ao pid,lstart,command | rg 'next dev|agent-builder-ui|ruh-frontend'`; `ps eww -p 58850`; `sed -n '1,220p' agent-builder-ui/.env.local`; `cat agent-builder-ui/.env.development.local`; `nl -ba agent-builder-ui/app/api/openclaw/route.ts | sed -n '147,207p'`; `sed -n '1,220p' agent-builder-ui/lib/openclaw/bridge-auth.ts`; `nl -ba agent-builder-ui/lib/openclaw/langfuse.ts | sed -n '66,123p'`
- Learning Note: `updated` [[LEARNING-2026-03-28-local-builder-bridge-auth-gap]]
- Blockers: `A repo-only local run still cannot emit bridge traces until the `/users/me` session-validation dependency is satisfied or the bridge adds a truthful development-only bypass.`

## 04:35 IST — Codex — Local builder bridge auth bypass and deterministic Langfuse flush
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/lib/openclaw/bridge-auth.ts`, `agent-builder-ui/lib/openclaw/bridge-auth.test.ts`, `agent-builder-ui/lib/openclaw/langfuse.ts`, `agent-builder-ui/lib/openclaw/langfuse.test.ts`, `agent-builder-ui/app/api/openclaw/route.ts`, `agent-builder-ui/app/api/openclaw/route.test.ts`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/specs/SPEC-agent-builder-bridge-auth.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-28-local-builder-bridge-auth-gap.md`
- Summary: `Closed the builder-side local Langfuse gap. The bridge auth helper now allows a narrow localhost-only development bypass when both the request host and configured backend URL are local and `NODE_ENV=development`, which lets repo-only `/api/openclaw` requests reach the traced path without weakening non-local auth. The Langfuse helper now keeps a shared `LangfuseSpanProcessor` and force-flushes completed traces before returning so short-lived local error paths do not depend on background export timing. Live verification used a temporary builder on `localhost:3003` with an intentionally bad gateway URL to force a fast terminal bridge result; the route completed with retries, the Langfuse SDK debug logs showed processed `openclaw.bridge.*` spans under one trace id, and the self-hosted OTLP endpoint on `http://localhost:3002/api/public/otel/v1/traces` enforced the seeded API key.`
- Verification: `cd agent-builder-ui && bun test lib/openclaw/langfuse.test.ts lib/openclaw/bridge-auth.test.ts app/api/openclaw/route.test.ts`; `kill 58820 58821 58850`; `cd agent-builder-ui && npm run dev -- --port 3001`; `curl -N -X POST http://localhost:3001/api/openclaw ...`; `./scripts/langfuse-local.sh down`; `./scripts/langfuse-local.sh up`; `curl -sS http://localhost:8124/ping`; `docker exec openclaw-langfuse-postgres-1 psql -U postgres -d postgres -c "select id, name, created_at from projects;"`; `docker exec openclaw-langfuse-postgres-1 psql -U postgres -d postgres -c "select id, public_key, project_id, created_at from api_keys;"`; `cd agent-builder-ui && LANGFUSE_DEBUG=true OPENCLAW_GATEWAY_URL=ws://127.0.0.1:1 OPENCLAW_GATEWAY_TOKEN=test node_modules/.bin/next dev --port 3003`; `curl -N -X POST http://localhost:3003/api/openclaw ...`; SDK debug log inspection showing processed `openclaw.bridge.request`, `openclaw.bridge.retry`, `openclaw.bridge.socket_error`, and `openclaw.bridge.socket_closed``; `curl -X POST http://localhost:3002/api/public/otel/v1/traces -H 'Authorization: Basic ...' --data '{}'` → `200`; same endpoint without auth or with bad auth → `401`
- Learning Note: `updated` [[LEARNING-2026-03-28-local-builder-bridge-auth-gap]]
- Blockers: `Builder-side tracing now reaches and flushes Langfuse in local development. Any remaining "empty Langfuse UI" symptom is downstream in the self-hosted Langfuse app/worker/storage path rather than the builder bridge auth boundary.`
