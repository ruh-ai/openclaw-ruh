# 2026-03-30 — Daily Journal

## 09:56 IST — Codex — Fail closed on Ship-stage activation blockers
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/lib/openclaw/copilot-flow.ts`, `agent-builder-ui/lib/openclaw/copilot-flow.test.ts`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/CoPilotLayout.tsx`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/LifecycleStepRenderer.tsx`, `agent-builder-ui/app/(platform)/agents/create/page.tsx`, `agent-builder-ui/app/(platform)/agents/[id]/chat/_components/TabChat.tsx`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-agent-builder-gated-skill-tool-flow.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-30-copilot-ship-runtime-input-readiness-gap.md`
- Summary: Debugged the linked Ship-stage activation error on `/agents/create?agentId=7653519b-c9cb-4269-a70c-5a94f2158a6d`. The real backend failure was `400 Missing required runtime inputs: HELPDESK_API_KEY, SLACK_WEBHOOK_URL`, but the frontend still treated missing required runtime inputs as advisory-only and the Ship-stage callback ignored `pushAgentConfig().ok === false`, so `Save & Activate` could progress into a false-success state after a rejected activation. The fix makes Co-Pilot deploy readiness fail closed on blank required runtime inputs, threads that same contract into the embedded Ship-stage CTA, and makes the page-level completion handler return an explicit success/failure result so Ship stops on a failed config push instead of pretending activation succeeded.
- Verification: `cd agent-builder-ui && bun test 'lib/openclaw/copilot-flow.test.ts'`; `cd agent-builder-ui && bun test 'app/(platform)/agents/create/_components/copilot/__tests__/lifecycle-stage-logic.test.ts'`; `cd agent-builder-ui && npx tsc --noEmit`; headless Playwright repro on `http://localhost:3001/agents/create?agentId=7653519b-c9cb-4269-a70c-5a94f2158a6d&devMockBar=1` confirmed both `Deploy Agent` and `Save & Activate` are disabled in Ship-stage state when required runtime inputs are blank and no activation request is fired; direct backend repro confirmed `POST /api/sandboxes/f579ade5-1eee-45a9-9156-ad558015bf2d/configure-agent` returns `400` with missing runtime-input details when those blank values are sent.
- Learning Note: `docs/knowledge-base/learnings/LEARNING-2026-03-30-copilot-ship-runtime-input-readiness-gap.md`
- Blockers: `The linked draft still contains blank required runtime inputs, so the user must fill those values before Ship can activate successfully; the code now blocks that state earlier instead of failing only after click.`

## 04:46 IST — Codex — Align build-stage generation with approved architecture plan
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/(platform)/agents/create/_config/generate-skills.ts`, `agent-builder-ui/app/(platform)/agents/create/_config/generate-skills.test.ts`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/CoPilotLayout.tsx`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-agent-builder-gated-skill-tool-flow.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-30-build-stage-architecture-plan-handoff.md`
- Summary: Tightened the create-flow build contract so the approved Plan stage now actually feeds the build output. `generateSkillsFromArchitect()` now receives the approved `architecturePlan`, injects that JSON into the build prompt, and explicitly requires `skill_graph.nodes[].skill_md` in the `ready_for_review` payload. `CoPilotLayout` passes the live plan into both initial build and retry flows, so build-stage output is anchored to the reviewed plan instead of re-inferring a detached graph from name/description alone. This directly addresses the linked forge draft symptom where the backend still showed `skills=[]` / `skill_graph=[]` after plan work because the old legacy helper was not carrying the reviewed build contract forward.
- Verification: `cd agent-builder-ui && bun test 'app/(platform)/agents/create/_config/generate-skills.test.ts'`; `cd agent-builder-ui && bun test 'lib/openclaw/copilot-state.test.ts' 'lib/openclaw/create-session-cache.test.ts' 'app/(platform)/agents/create/_components/copilot/__tests__/lifecycle-stage-logic.test.ts'`; `cd agent-builder-ui && npx tsc --noEmit`; `curl -sS http://localhost:8000/api/agents/7653519b-c9cb-4269-a70c-5a94f2158a6d`
- Learning Note: `docs/knowledge-base/learnings/LEARNING-2026-03-30-build-stage-architecture-plan-handoff.md`
- Blockers: `Live browser verification is still limited by the local Playwright/Chrome session collision, so this pass relies on prompt regression coverage plus direct backend inspection rather than an end-to-end browser replay.`

## 04:22 IST — Codex — Unblock forge-backed draft autosave
- Status: `completed`
- Areas: `TODOS.md`, `ruh-backend/src/validation.ts`, `ruh-backend/tests/unit/validation.test.ts`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-agent-create-session-resume.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-30-forging-status-draft-autosave-gap.md`
- Summary: Fixed the `/agents/create?agentId=...` draft-save failure for forge-backed agents. Builder autosave already preserved the persisted agent status, but create-flow agents stay in `forging` while their dedicated forge sandbox is active; backend metadata validation still only allowed `active` or `draft`, so normal autosave PATCHes failed with `422` even though the store and UI already modeled `forging` as valid. The validator now accepts `forging`, and the KB/specs record that forge-backed autosave must preserve truthful lifecycle state rather than spoofing another status.
- Verification: `bun test ruh-backend/tests/unit/validation.test.ts`; `bun test agent-builder-ui/hooks/use-agents-store.test.ts --filter 'saveAgentDraft re-fetches an existing draft when the local store is cold after refresh'`; `curl -sS -D - -X PATCH http://localhost:8000/api/agents/7653519b-c9cb-4269-a70c-5a94f2158a6d -H 'Content-Type: application/json' --data '{"name":"Google ads manager","description":"manages google ads api","skills":[],"triggerLabel":"","status":"forging","channels":[]}'`; `curl -sS -D - -X PATCH http://localhost:8000/api/agents/7653519b-c9cb-4269-a70c-5a94f2158a6d/config -H 'Content-Type: application/json' --data '{"skillGraph":null,"workflow":null,"agentRules":[],"toolConnections":[],"triggers":[],"improvements":[]}'`
- Learning Note: `docs/knowledge-base/learnings/LEARNING-2026-03-30-forging-status-draft-autosave-gap.md`
- Blockers: `Browser-level repro was not re-run because the local Playwright MCP launcher is still blocked by an existing Chrome session, but the exact failing autosave PATCH now succeeds against the live dev backend.`

## 04:21 IST — Codex — Hide create-flow dev mock bar by default
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/(platform)/agents/create/page.tsx`, `agent-builder-ui/app/(platform)/agents/create/dev-mock-bar.ts`, `agent-builder-ui/app/(platform)/agents/create/dev-mock-bar.test.ts`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/specs/SPEC-copilot-config-workspace.md`
- Summary: Tightened the `/agents/create` debug chrome so the yellow DEV mock-stage strip no longer shows up by default during normal local usage. The helper now fails closed behind a small visibility function and only renders when the route is opened with `?devMockBar=1`, which keeps the standard Co-Pilot layout clean while preserving explicit stage-mock access for debugging.
- Verification: `cd agent-builder-ui && bun test 'app/(platform)/agents/create/dev-mock-bar.test.ts'`; `cd agent-builder-ui && npm run typecheck`
- Learning Note: `None`
- Blockers: `Browser smoke was not rerun because the local Playwright launcher is still colliding with an existing Chrome session.`

## 22:35 IST — Codex — Restore create-flow drafts and forge state after refresh
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/(platform)/agents/create/page.tsx`, `agent-builder-ui/hooks/use-agents-store.ts`, `agent-builder-ui/hooks/use-agents-store.test.ts`, `agent-builder-ui/lib/openclaw/create-session-cache.ts`, `agent-builder-ui/lib/openclaw/create-session-cache.test.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-agent-create-session-resume.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-30-create-agent-refresh-hydration-gap.md`
- Summary: Fixed the builder refresh/resume gap on `/agents/create`. The create page now re-fetches the route agent from the backend on mount, restores a safe local create-session cache for Co-Pilot + builder state, keeps the old lifecycle cache as a fallback, and clears both caches on completion or discard. `saveAgentDraft()` was also hardened so a resumed autosave fetches the backend agent when the local Zustand store is cold after reload instead of failing the draft save loop.
- Verification: `bun test agent-builder-ui/lib/openclaw/create-session-cache.test.ts agent-builder-ui/hooks/use-agents-store.test.ts agent-builder-ui/lib/openclaw/copilot-lifecycle-cache.test.ts`; `cd agent-builder-ui && npx tsc --noEmit`; browser smoke on `http://localhost:3001/agents/create?agentId=d70d1c31-3a6e-4552-8419-ad8fe36233fd` confirmed the page rehydrated the saved draft, surfaced `Draft saved`, and restored the persisted PRD/TRD state after the post-mount backend fetch.
- Learning Note: `docs/knowledge-base/learnings/LEARNING-2026-03-30-create-agent-refresh-hydration-gap.md`
- Blockers: `The historical agent `d70d1c31-3a6e-4552-8419-ad8fe36233fd` still has no linked sandbox in local backend data (`sandbox_ids=[]`, `forge_sandbox_id=null`), so this fix restores its saved draft state but cannot invent a missing deployment record.`

## 03:36 IST — Codex — Fix backend startup worker-cost migration mismatch
- Status: `completed`
- Areas: `TODOS.md`, `ruh-backend/src/schemaMigrations.ts`, `ruh-backend/tests/unit/schemaMigrations.test.ts`, `ruh-backend/tests/integration/schemaMigrations.test.ts`, `docs/knowledge-base/005-data-models.md`, `docs/knowledge-base/specs/SPEC-backend-schema-migrations.md`, `docs/knowledge-base/learnings/LEARNING-2026-03-30-worker-cost-tracking-agent-id-type-mismatch.md`
- Summary: Fixed a startup-blocking schema migration bug in `0022_worker_cost_tracking`. The migration had declared `cost_events`, `budget_policies`, and `execution_recordings` with `agent_id UUID REFERENCES agents(id)` even though `agents.id` is `TEXT`, so Postgres refused the foreign keys and `ruh-backend` never reached port `8000`. The fix normalizes all three foreign-key columns to `TEXT`, adds unit and real-DB regression coverage, and documents the canonical rule in the KB.
- Verification: `cd ruh-backend && bun test tests/unit/schemaMigrations.test.ts`; `cd ruh-backend && NODE_ENV=development TEST_DATABASE_URL='postgresql://openclaw:changeme@localhost:5432/codex_schema_test_1774821575' bun test tests/integration/schemaMigrations.test.ts`; `cd ruh-backend && bun run dev`; `curl -sS http://127.0.0.1:8000/ready`
- Learning Note: `docs/knowledge-base/learnings/LEARNING-2026-03-30-worker-cost-tracking-agent-id-type-mismatch.md`
- Blockers: `None`

## 18:25 IST — Codex — Refresh Agent's Computer terminal shell
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/(platform)/agents/[id]/chat/_components/TabChat.tsx`, `agent-builder-ui/e2e/tab-chat-terminal.spec.ts`, `docs/plans/2026-03-30-agent-computer-terminal-shell-refresh-design.md`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/specs/SPEC-agent-computer-terminal-shell.md`
- Summary: Reworked the shared terminal inside `Agent's Computer` so it now uses a provisioning-style dark shell with bounded chrome, a scrollable command body, and an embedded prompt bar instead of a detached page-footer input. The change lands in the shared `TabChat.tsx` terminal panel, so both builder and deployed-agent chat inherit the refreshed terminal presentation.
- Verification: `cd agent-builder-ui && npx tsc --noEmit`; `cd agent-builder-ui && npx playwright test e2e/tab-chat-terminal.spec.ts --grep "shows tool step and terminal command for <function=> tool call"`; visual screenshot captured at `/tmp/tabchat-terminal-refresh.png`.
- Learning Note: `None`
- Blockers: `None`

## 19:05 IST — Codex — Keep create-flow workspace tabs static
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/(platform)/agents/[id]/chat/_components/TabChat.tsx`, `agent-builder-ui/app/(platform)/agents/[id]/chat/_components/tab-workspace-autoswitch.ts`, `agent-builder-ui/app/(platform)/agents/[id]/chat/_components/tab-workspace-autoswitch.test.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-copilot-config-workspace.md`, `docs/knowledge-base/specs/SPEC-create-flow-static-workspace-tabs.md`
- Summary: Changed the `/agents/create` Co-Pilot workspace so it no longer auto-switches tabs while builder activity is streaming. The new pure `tab-workspace-autoswitch` policy keeps builder mode static and preserves the existing deployed-agent auto-switch behavior, and the KB/specs now describe the create-flow workspace as fully user-controlled during the run.
- Verification: `cd agent-builder-ui && bun test "app/(platform)/agents/[id]/chat/_components/tab-workspace-autoswitch.test.ts" "app/(platform)/agents/create/_components/copilot/__tests__/lifecycle-stage-logic.test.ts"`; `cd agent-builder-ui && npx tsc --noEmit`.
- Learning Note: `None`
- Blockers: `None`

## 03:44 IST — Codex — Keep builder terminal runs out of transcript
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/(platform)/agents/[id]/chat/_components/TabChat.tsx`, `agent-builder-ui/lib/openclaw/ag-ui/use-agent-chat.ts`, `agent-builder-ui/lib/openclaw/ag-ui/types.ts`, `agent-builder-ui/lib/openclaw/ag-ui/run-surface-policy.ts`, `agent-builder-ui/lib/openclaw/ag-ui/__tests__/run-surface-policy.test.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-builder-terminal-transcript-isolation.md`
- Summary: Split terminal-origin builder runs from normal chat-origin runs. Commands entered from the Agent's Computer terminal in `/agents/create` now execute as workspace-only activity, so the left transcript no longer echoes the synthetic terminal instruction or streamed output. The right-side terminal/task history still works because completed workspace runs keep their structured replay data on hidden transcript messages.
- Verification: `cd agent-builder-ui && bun test 'lib/openclaw/ag-ui/__tests__/run-surface-policy.test.ts'`; `cd agent-builder-ui && npx tsc --noEmit`
- Learning Note: `None`
- Blockers: `None`

## 19:22 IST — Codex — Finish builder thinking steps when reasoning ends
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/lib/openclaw/ag-ui/use-agent-chat.ts`, `agent-builder-ui/lib/openclaw/ag-ui/event-consumer-map.ts`, `agent-builder-ui/lib/openclaw/ag-ui/reasoning-step.ts`, `agent-builder-ui/lib/openclaw/ag-ui/__tests__/reasoning-step.test.ts`, `agent-builder-ui/lib/openclaw/ag-ui/__tests__/event-consumer-map.test.ts`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/specs/SPEC-agui-protocol-adoption.md`
- Summary: Fixed the stuck create-flow thinking indicator by unifying reasoning-step lifecycle tracking. Custom `reasoning` events and AG-UI `REASONING_*` events now share one persistent mutable step-id ref via `reasoning-step.ts`, so the same thinking row is started, updated, and finished instead of leaving the footer and reasoning list on an orphaned active step.
- Verification: `cd agent-builder-ui && bun test lib/openclaw/ag-ui/__tests__/reasoning-step.test.ts lib/openclaw/ag-ui/__tests__/event-consumer-map.test.ts`; `cd agent-builder-ui && npx tsc --noEmit`.
- Learning Note: `None`
- Blockers: `None`

## 04:02 IST — Codex — Ground builder refine loop in current agent state
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/app/(platform)/agents/[id]/chat/_components/TabChat.tsx`, `agent-builder-ui/lib/openclaw/ag-ui/builder-agent.ts`, `agent-builder-ui/lib/openclaw/ag-ui/event-consumer-map.ts`, `agent-builder-ui/lib/openclaw/wizard-directive-parser.ts`, `agent-builder-ui/lib/openclaw/builder-chat-suggestions.ts`, `agent-builder-ui/lib/openclaw/ag-ui/__tests__/builder-agent.test.ts`, `agent-builder-ui/lib/openclaw/ag-ui/__tests__/event-consumer-map.test.ts`, `agent-builder-ui/lib/openclaw/wizard-directive-parser.test.ts`, `agent-builder-ui/lib/openclaw/builder-chat-suggestions.test.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-builder-contextual-refine-loop.md`
- Summary: Fixed three linked `/agents/create` issues. The Think stage no longer flips back to loading when later-stage builder chat runs fire, empty-state prompt chips now become stage-aware prompts derived from the current agent name/description instead of unrelated canned examples, and post-build architect chat runs now use a dedicated refine-mode instruction plus a richer `[WIZARD_STATE]` payload that includes tools, runtime inputs, heartbeat/triggers, channels, architecture-plan summary, and SOUL summary.
- Verification: `cd agent-builder-ui && bun test 'lib/openclaw/ag-ui/__tests__/builder-agent.test.ts'`; `cd agent-builder-ui && bun test 'lib/openclaw/ag-ui/__tests__/event-consumer-map.test.ts'`; `cd agent-builder-ui && bun test 'lib/openclaw/wizard-directive-parser.test.ts'`; `cd agent-builder-ui && bun test 'lib/openclaw/builder-chat-suggestions.test.ts'`; `cd agent-builder-ui && npx tsc --noEmit`
- Learning Note: `None`
- Blockers: `Browser MCP sanity-check was skipped because the local Playwright launcher could not start Chrome and exited with "Opening in existing browser session."`

## 04:22 IST — Codex — Preserve forward lifecycle navigation after refresh
- Status: `completed`
- Areas: `TODOS.md`, `agent-builder-ui/lib/openclaw/copilot-state.ts`, `agent-builder-ui/lib/openclaw/copilot-state.test.ts`, `agent-builder-ui/lib/openclaw/copilot-lifecycle-cache.ts`, `agent-builder-ui/lib/openclaw/copilot-lifecycle-cache.test.ts`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/LifecycleStepRenderer.tsx`, `agent-builder-ui/app/(platform)/agents/create/_components/copilot/__tests__/lifecycle-stage-logic.test.ts`, `docs/knowledge-base/000-INDEX.md`, `docs/knowledge-base/008-agent-builder-ui.md`, `docs/knowledge-base/011-key-flows.md`, `docs/knowledge-base/specs/SPEC-create-flow-lifecycle-navigation.md`
- Summary: Fixed the create-flow stepper regression that appeared after refresh-resume on Review. The Co-Pilot store now keeps `maxUnlockedDevStage` separate from the currently viewed `devStage`, so clicking Build/Plan/Think to inspect earlier work no longer locks Review or forces the agent back into plan/build regeneration. The footer `Back` button remains the destructive rewind path, and the lifecycle cache now preserves the furthest unlocked stage across refreshes.
- Verification: `cd agent-builder-ui && bun test lib/openclaw/copilot-state.test.ts lib/openclaw/copilot-lifecycle-cache.test.ts 'app/(platform)/agents/create/_components/copilot/__tests__/lifecycle-stage-logic.test.ts'`; `cd agent-builder-ui && npx tsc --noEmit`
- Learning Note: `None`
- Blockers: `No live browser rerun in this pass; the fix was verified through focused store/stepper/cache regressions and typecheck.`
