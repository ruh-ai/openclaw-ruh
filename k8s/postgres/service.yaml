apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: openclaw
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP — only reachable inside the cluster
  type: ClusterIP
