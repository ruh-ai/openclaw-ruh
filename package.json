{
  "name": "openclaw-ruh-enterprise",
  "private": true,
  "description": "Ruh.ai — Digital employees with a soul",
  "scripts": {
    "test:all": "npm run test:backend && npm run test:agent-builder && npm run test:frontend && npm run test:admin && npm run test:marketplace",
    "test:backend": "cd ruh-backend && bun run test:unit && bun test tests/contract/",
    "test:agent-builder": "cd agent-builder-ui && bun test lib/openclaw/api.test.ts && bun test lib/ hooks/ app/ services/ middleware.test.ts --path-ignore-patterns '**/api.test.ts'",
    "test:frontend": "cd ruh-frontend && npx jest --ci --passWithNoTests",
    "test:admin": "cd admin-ui && bun test",
    "test:marketplace": "cd packages/marketplace-ui && bun test",
    "test:contract": "cd ruh-backend && bun test tests/contract/",
    "test:integration": "cd ruh-backend && bun test tests/integration/ --timeout 15000",
    "test:e2e:admin": "cd admin-ui && npx playwright test",
    "coverage:all": "npm run coverage:backend && npm run coverage:agent-builder && npm run coverage:frontend && npm run coverage:admin && npm run coverage:marketplace",
    "coverage:backend": "cd ruh-backend && bun run test:coverage",
    "coverage:agent-builder": "cd agent-builder-ui && bun test --coverage --coverage-reporter=lcov && bun run scripts/check-coverage.ts",
    "coverage:frontend": "cd ruh-frontend && npx jest --ci --coverage",
    "coverage:admin": "cd admin-ui && bun test --coverage --coverage-reporter=lcov && bun run scripts/check-coverage.ts",
    "coverage:marketplace": "cd packages/marketplace-ui && bun test --coverage --coverage-reporter=lcov && bun run scripts/check-coverage.ts",
    "coverage:report": "echo '=== Coverage Report ===' && npm run coverage:backend 2>&1 | grep '\\[coverage\\]' && npm run coverage:agent-builder 2>&1 | grep '\\[coverage\\]' && npm run coverage:frontend 2>&1 | grep -E 'Stmts|Lines|coverage' && npm run coverage:admin 2>&1 | grep '\\[coverage\\]' && npm run coverage:marketplace 2>&1 | grep '\\[coverage\\]'",
    "typecheck:all": "npm run typecheck:backend && npm run typecheck:agent-builder && npm run typecheck:admin && npm run typecheck:marketplace",
    "typecheck:backend": "cd ruh-backend && npx tsc --noEmit",
    "typecheck:agent-builder": "cd agent-builder-ui && npx tsc --noEmit",
    "typecheck:admin": "cd admin-ui && npx tsc --noEmit",
    "typecheck:marketplace": "cd packages/marketplace-ui && npx tsc --noEmit",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "pino": "^10.3.1"
  }
}
